---
name: "Antigravity CLI"
description: "Terminal interface for Google's Antigravity CLI (agy) with deep MCP integration"
version: "3.0.0"
slug: "gemini_terminal"
url: "https://github.com/oded996/gemini-cli-home-assistant-addons"
arch:
  - aarch64
  - amd64
startup: application
init: false

# Ingress configuration
ingress: true
ingress_port: 8099
ingress_stream: true
panel_icon: mdi:google-gemini
panel_title: "Antigravity CLI"

# File access - Home Assistant config directory
map:
  - config:rw

# API access
hassio_api: true
hassio_role: manager
homeassistant_api: true

# Security
host_network: false
privileged: []
apparmor: true

# User-configurable options
options:
  gemini_api_key: ""
  enable_ha_mcp: true
  persistent_apk_packages: []
  persistent_pip_packages: []

  # Terminal appearance
  terminal_theme: "breeze"
  font_size: 14
  cursor_style: "block"
  cursor_blink: false

  # Screenshot tool: enables visual verification of dashboards and UI changes.
  screenshot_enabled: false

  # Long-lived access token for direct HA Core API access.
  access_token: ""

  # Zigbee2MQTT configuration
  z2m_url: ""
  z2m_mqtt_topic: "zigbee2mqtt"

  # Environment variables to inject into the container
  env_vars: []

schema:
  gemini_api_key: password?
  enable_ha_mcp: bool?
  persistent_apk_packages:
    - str
  persistent_pip_packages:
    - str

  # Terminal appearance
  terminal_theme: list(breeze|catppuccin_mocha|catppuccin_latte|dracula|nord|tokyo_night|one_dark|solarized_dark|solarized_light|gruvbox_dark)
  font_size: int(10,24)
  cursor_style: list(block|underline|bar)
  cursor_blink: bool

  # Screenshot tool
  screenshot_enabled: bool

  # HA Core access token
  access_token: password?

  # Zigbee2MQTT (optional)
  z2m_url: str?
  z2m_mqtt_topic: str?

  # Environment variables
  env_vars:
    - name: str
      value: str

# Exclude caches from backups
backup_exclude:
  - ".npm/*"
  - ".cache/*"
  - ".local/share/antigravity/cache/*"
  - ".local/share/gemini/cache/*"
  - ".antigravity/cache/*"
  - ".env_vars"
